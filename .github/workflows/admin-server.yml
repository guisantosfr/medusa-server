name: Faça uma requisição no Render a cada 14 minutos

on:
  schedule:
    - cron: "*/14 * * * *"

jobs:
  render-request:
    runs-on: ubuntu-latest
    steps:
      - name: Get Products
        run: curl https://medusa-server-9q7c.onrender.com/store/products
